# Checks de qualité des données pour MongoDB
checks for staging_data:
  - row_count > 0
  - missing_count(video_id) = 0
  - duplicate_count(video_id) = 0

checks for core_data:
  - row_count > 0
  - missing_count(video_id) = 0
  - missing_count(title) = 0
  - duplicate_count(video_id) = 0
  - invalid_count(view_count) = 0:
      valid min: 0
  - invalid_count(like_count) = 0:
      valid min: 0
  - invalid_count(comment_count) = 0:
      valid min: 0

checks for history_data:
  - row_count > 0
  - missing_count(video_id) = 0
  - missing_count(valid_from) = 0
